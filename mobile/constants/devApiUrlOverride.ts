/**
 * If sign-in still cannot reach Next.js, set this to your dev URL (same host/port as “Local:” in
 * the Next terminal). Only used in __DEV__.
 *
 * Examples:
 * - Physical device (LAN): "http://192.168.0.152:3001"
 * - Android emulator (host loopback): "http://10.0.2.2:3001"
 */
export const DEV_API_URL_OVERRIDE = "";
